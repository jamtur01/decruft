# MathJax v3 SVG rendering

We define the reward as rperf.

The full formula is:

rPARL(x,y)\=λ1⋅rparallel+λ2⋅rfinish+rperf(x,y)

This is a VitePress blog using MathJax SVG rendering without embedded LaTeX source.